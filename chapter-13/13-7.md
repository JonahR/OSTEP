pmap of a.out reveals that there is mappings for the binary (a.out),
the standard C library, the operating system (linux x86), the stack, and
the heap. There are also anon mappings that I do not know what they are.
Either the a.out mapping or the anon mapping relates to the code and
read only data. 